# Package actions for Calamares.
# The full profile-selection path is still being validated. The build system
# already supports profile-specific images with VANTAOS_PROFILE.

backend: apt
operations:
  - remove:
    - ubiquity
    - ubiquity-frontend-gtk
  - install:
    - vantaos-branding
    - vantaos-default-settings
