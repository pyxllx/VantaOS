# APT Preferences

Keep this directory for future pinning.

VantaOS Obsidian should avoid aggressive PPAs and third-party repositories in
the base image. Prefer Ubuntu 24.04 repositories first, Flatpak second, and
external repositories only when the maintenance tradeoff is worth it.
